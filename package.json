{
  "name": "aqueous-humanizer-ai",
  "version": "1.0.0",
  "description": "Humanizer AI website with email accounts, daily free trial, premium subscription, and PayChangu checkout hooks.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
